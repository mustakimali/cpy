# include "math_utils.tc"

func add(a, b) {
    return a + b;
}

func factorial(n) {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}


let result = add(5, 3);
print(result);           # 8
print(factorial(5));     # 120
